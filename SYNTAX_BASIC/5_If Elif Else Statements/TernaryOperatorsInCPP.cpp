#include <bits/stdc++.h>
#define ll long long
#define MOD 1000000007
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    for (int i = 0; i < 10; i++){
        (i < 5) ? cout << "i less than 5" : cout << "i greater than 5 and less than 10";
        cout << endl;
    }
    return 0;
}